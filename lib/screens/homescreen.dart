import "package:flutter/material.dart";

class HomseScreen extends StatelessWidget {
  const HomseScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Brainbatu'),
      ),
    );
  }
}
